package configs.barcodes

/**
 * 按位转换为ASCII码，第二个参数未使用，只是表意
 * Created by xiuyou.xu on 2017/10/10.
 */
StringBuilder sb = new StringBuilder();
for (int i = 0; i < value.length(); i++) {
    sb.append((int) value.charAt(i));
}
return sb.toString();